# 실시간 시각 폐회로와 공룡게임 — 측정 기록

측정 환경: Apple M5, 10코어, 24 GB, mlx 0.32.2, Python 3.14, FlyWire v783 (138,639 뉴런).
이 문서는 "무엇이 되고 무엇이 안 되는지"를 숫자로 남긴 기록이다. 실패한 시도도 적었다.

## 1. 실시간은 이미 달성되어 있었다

| 엔진 | 조건 | RTF (벽시계/생물시간) |
|---|---|---|
| MLX (GPU) | 무자극 | 0.50x |
| MLX (GPU) | R1-6 7,932개 @ 20 Hz | 0.45x |
| MLX (GPU) | @ 100 Hz (망 평균 5.7 Hz) | 0.46x |
| MLX (GPU) | @ 300 Hz (망 평균 17.2 Hz) | 0.46x |
| numpy (CPU) | 무자극 | 2.17x |

- MLX 엔진은 실시간의 약 2.2배 빠르다. RTF 이 발화량과 거의 무관한데, propagate 커널 비용이
  실제 스파이크 수가 아니라 전체 시냅스(14.76 M)를 64개씩 쪼갠 작업 항목 수에 지배되기 때문이다.
- `run(eval_every=)` 은 64 이상이어야 한다. 1 이면 커널 launch/sync 오버헤드로 RTF 2.35x (5배 느림).
- 실제 게임 폐회로에서 프레임당 작업은 p50 12.1 ms / p95 12.3 ms (예산 16.67 ms = 60 fps).
  벽시계에 맞춰 돌리면 p50 14.7 ms (GPU 가 프레임 사이에 쉬면서 클럭이 내려간다). 둘 다 예산 안.

## 2. 망막지도는 복구된다

R1-6 은 soma 좌표가 없다(망막이 촬상 볼륨 밖). Mi1 이 메둘라 컬럼당 1개이고 좌표가 거의
완비(1,555/1,584)이므로 `R1-6 → L1/L2/L3 → Mi1` 연결로 광수용체를 컬럼에 배정한다.
컬럼 방향은 Mi1 세포체층에 구를 맞춰(반지름 150~166 µm, 잔차 5~13 µm) 중심에서 본 방향으로 쓴다.

### 해부학만으로 HRC 공간 offset 이 확인된다

T4/T5 의 빠른 가지와 느린 가지가 서로 다른 컬럼에서 오는지, 그 방향이 아유형별로 일관되는지를
시뮬레이션 없이 잰 결과 (왼쪽 눈, offset = 느린 가지 위치 − 빠른 가지 위치):

| 뉴런 | 빠름→느림 | offset (방위각, 고도) | 크기 | 일관성 |
|---|---|---|---|---|
| T4a | Mi1→Mi9 | (+4.02, −0.71) | 4.08° | 0.50 |
| T4b | Mi1→Mi9 | (−1.87, +1.47) | 2.38° | 0.35 |
| T4c | Mi1→Mi9 | (−0.59, −1.97) | 2.05° | 0.29 |
| T4d | Mi1→Mi9 | (+1.00, +3.36) | 3.51° | 0.45 |
| T5a | Tm2→Tm9 | (+4.43, −0.94) | 4.53° | 0.59 |
| T5d | Tm2→Tm9 | (+2.24, +2.62) | 3.45° | 0.49 |

a/b 는 방위각 축에서 서로 반대, c/d 는 고도 축에서 서로 반대다 — Maisak et al. 2013 의
교과서 구성 그대로다. 대조군(둘 다 빠른 가지인 Mi1 vs Tm3)은 offset 0.47° 로 8.7배 작다.
컬럼 간격이 2.25° 이므로 offset 2~4.5° 는 1~2 컬럼, HRC 가 요구하는 최근접 offset 이다.

**배선과 부호는 맞다.** T4a 입력은 Mi1 +30%, Tm3 +10% (빠른 흥분) / Mi9 −8.4%, CT1 −6.8%,
Mi4 −4.4% (느린 억제). T5a 는 Tm9 +16%, Tm2 +15%, Tm1 +12% / CT1 −13.5%.
전달물질도 Mi4=GABA, CT1=GABA, Mi9=글루탐산으로 문헌과 같다.

## 3. 그런데 방향 선택성은 나오지 않는다 (실패 기록)

시도한 것과 결과:

| 시도 | 결과 |
|---|---|
| 균일 자극 (기존 방식) | 시엽 전체 침묵. R1-6 은 히스타민 = 100% 억제(26,928 시냅스, 흥분 0)라 라미나를 켤 수 없다 |
| 시엽 내재뉴런 77,530개에 지속 전류(tonic bias) 항상성 보정 | 경로는 살아남. 하지만 DNp01 이 빈 화면에서도 34~47 Hz 로 지속 발화(범람) |
| 시냅스 풀 2개 (Mi9/Mi4/CT1/Tm9 를 느린 풀로) | T4a DSI: 대조군 +0.017 → 느린 풀 −0.020. 개선 없음 |
| 느린 시정수 30/60/100 ms × 세기 보존/0.3/1.0 스윕 | 전부 DSI ≈ 0 |
| 등급전위(rate) 모델, 포화 비선형 | 네 방향이 소수점 4자리까지 동일. 사실상 선형 영역 |
| 등급 + 분할(shunting) 억제 gi = 3/10/30 | 여전히 DSI = 0.000 |
| 뉴런별 DSI + 자기 해부학 offset 과의 상관 | \|DSI\| 중앙값 0.002, 상관 cos = −0.02~+0.035 (무상관) |

실제 파리 T4/T5 의 DSI 는 0.5~0.9 이다. 여기서는 0.002.

**원인.** 이 모델은 모든 뉴런이 같은 막 시정수(20 ms)·시냅스 시정수(5 ms)·시냅스 지연(1.8 ms)을
쓰고, 억제가 전류 기반 **감산** 억제다. HRC 는 두 가지를 곱하거나 강한 비선형으로 결합해야
평균 반응에 운동 신호가 남는다. 시정수 비대칭을 넣어도(위 3번째 줄) 동작점이 선형 영역에
머물면 demodulation 이 일어나지 않는다. 게다가 시엽 컬럼 뉴런 대부분(L1~L5, Mi, Tm, Dm, CT1)은
원래 스파이크를 내지 않는 등급전위 뉴런이라 스파이킹 LIF 근사 자체가 맞지 않는다.

→ 커넥톰에서 T4/T5 방향 선택성을 끌어내려면 시엽 전용 모델(등급전위, 컨덕턴스 기반 분할 억제,
세포유형별 동작점)을 제대로 만들어야 한다. 그것은 별도의 연구 과제다.

## 4. 그래서 게임을 파리가 풀 수 있는 문제로 바꿨다

원래 크롬 공룡게임은 측면 스크롤이라 선인장이 확대되지 않는다(측면 이동 = 방향 선택성 필요).
**공룡 1인칭 시점**으로 렌더링하면 다가오는 선인장이 각크기가 커지는 **루밍** 자극이 되고,
루밍은 실제 초파리에서 도피 도약을 일으키는 자연 유발자극이다.

도약 신호는 **중심 − 주변 라미나 발화율**로 읽는다. 어두운 물체가 정면 수용장에 들어오면
그 컬럼의 광수용체(히스타민 = 억제)가 조용해지고 라미나 단극세포가 탈억제되어 발화가 오른다.
전역 밝기 변화는 중심과 주변에 같이 나타나므로 빼면 상쇄된다. LC/LPLC 가 하는 국소 풀링과
같은 구조를 디코더에서 만든 것이다.

- 중심: 지도 중심 25° 안의 L1/L2/L3/Tm1/Tm2 (269개)
- 주변: 35~75° 고리의 같은 유형 (4,922개)
- 지표를 1초 이동평균으로 뺀 뒤(순응) 임계값 1.8, 불응기 0.45초

### 결과 (6 시행 × 3 조건, 각 20초)

| 조건 | 생존 평균 | 점수 평균 | 점수 합 | 완주 |
|---|---|---|---|---|
| **정상(시각)** | **17.9초** | **6.5** | **39** | **5/6** |
| 맹목(게임은 돌되 균일 회색만 보여줌) | 2.85초 | 0.0 | 0 | 0/6 |
| R1-6 침묵 | 2.85초 | 0.0 | 0 | 0/6 |

두 음성 대조군 모두 6/6 전부 0점으로 첫 선인장에서 죽는다. 벽시계 실시간 30초 실행에서도
선인장 13개를 넘으며 완주했다(프레임 작업 p50 14.7 ms / 예산 16.67 ms).

**정직하게 말하면**: 도약은 LPLC2 → DNp01(거대섬유) 경로가 아니라 시엽 집단 활동을 읽는
디코더에서 나온다. LPLC2 는 어떤 설정에서도 사실상 침묵이고 루밍 선택성이 없었으며
(loom 1.11 Hz vs 멀어지는 원 0.98 Hz), DNp01 은 자극과 무관하게 34~47 Hz 로 발화했다.

## 5. 재현 방법

```bash
uv run flybrain dino --duration 30                          # 실시간 1회
uv run flybrain dino --duration 20 --trials 6 --controls    # 대조군 배터리
uv run flybrain dino --blind                                # 맹목 대조군만
uv run flybrain dino --silence-type R1-6                    # 광수용체 침묵
```

결과는 `results/dino*.html` (재생기), `results/dino*_trials.csv`, `*_trajectory.csv` 에 남는다.

## 6. 측정 중 발견한 함정

- `VisualBrain` 을 시행 사이에 `net.reset()` 만으로 되돌리면 누적 발화수는 0 이 되는데
  `_prev` 가 이전 값을 들고 있어 첫 프레임 발화율이 거대한 음수가 된다. 이 때문에 초기
  스윕에서 "한 설정만 성공하고 나머지는 전부 같은 시각에 실패"하는 가짜 결과가 나왔다.
  `VisualBrain.reset()` 을 쓰면 된다.
- 작은 그룹의 프레임별 발화율은 심하게 양자화된다(정면 L1 18개 → 7.49 Hz 단위,
  DNp01 2개 → 30 Hz 단위). 디코더는 중심/주변을 각각 하나로 풀링해서 읽는다.
