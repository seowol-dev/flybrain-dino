# 실시간 실행 + 크롬 공룡게임 — 측정 결과와 구현 계획

작성: 2026-09-24. 측정 환경: Apple M5, 10코어, 24GB, mlx 0.32.2, Python 3.14.

## 1. 측정 결과 (추측 아님, 실측)

### 실시간은 이미 달성되어 있다

| 엔진 | 조건 | RTF (wall/bio) |
|---|---|---|
| MLX (GPU) | 무자극 | **0.50x** |
| MLX (GPU) | R1-6 7,932개 @ 20 Hz | **0.45x** |
| MLX (GPU) | R1-6 7,932개 @ 100 Hz (평균 5.7 Hz) | **0.46x** |
| MLX (GPU) | R1-6 7,932개 @ 300 Hz (평균 17.2 Hz) | **0.46x** |
| numpy (CPU) | 무자극 | 2.17x |

- **MLX 엔진은 실시간의 약 2.2배 빠르다.** 여유 시간이 54% 있다.
- RTF이 발화량과 거의 무관하다. propagate 커널 비용이 실제 스파이크 수보다
  전체 시냅스(14.76M)를 64개씩 쪼갠 작업 항목 수에 지배되기 때문이다.
  → 폐회로 오버헤드를 붙일 예산이 충분하다.
- `eval_every` 는 64 이상이어야 한다. `eval_every=1` 이면 RTF 2.35x 로 5배 느려진다
  (커널 launch/sync 오버헤드). 64/256/1024 는 차이 없음(0.44~0.60x).
- **README 의 "생물 시간 1초당 3~5초" 는 numpy CPU 엔진 기준이다. 수정 필요.**

### 회피 회로 전체가 커넥톰에 들어 있다

| 뉴런 | 개수 | soma 좌표 |
|---|---|---|
| R1-6 광수용체 | 7,932 | **없음 (NaN)** — 망막은 촬상 볼륨 밖 |
| L1 / L2 / L3 (라미나) | 1,591 / 1,697 / 1,442 | 1,311 / 1,412 / 1,212 |
| Mi1 (ON) / Tm1 (OFF) | 1,584 / 1,555 | 1,555 / 1,543 |
| T4a (ON 운동) / T5a (OFF 운동) | 1,462 / 1,484 | 전부 |
| LPLC2 / LC4 (루밍 검출) | 210 / 104 | 전부 |
| DNp01 (거대섬유) / DNp11 | 2 / 2 | 전부 |

즉 실제 초파리 탈출 경로가 그대로 있다:

```
화면 → R1-6 → L1/L2 → Mi1/Tm1 → T4/T5 → LPLC2/LC4 → DNp01(GF) → 점프
       (히스타민, 억제)      (ON/OFF 분리)  (운동검출)   (루밍)    (탈출)
```

## 2. 남은 진짜 문제는 속도가 아니라 시각이다

README 의 한계 지적이 정확하다: 지금은 광수용체 **전체를 같은 세기로** 자극한다.
T4/T5 는 시공간 대비(망막 컬럼을 가로지르는 명암 경계의 이동)에만 반응하므로
균일 조명으로는 원리적으로 구동되지 않는다. 그래서 망막지도가 필요하다.

### 망막지도 복구 방법 (광수용체에 좌표가 없으므로 연결로 추정)

1. **Mi1 을 컬럼 격자로 쓴다.** Mi1 은 메둘라 컬럼당 1개이고 좌표가 1,555/1,584 완비다.
2. `R1-6 → L1/L2/L3 → Mi1` 경로로 각 광수용체를 컬럼에 배정한다.
   (실측: 광수용체 7,932개 중 6,857개가 L1/L2/L3 에 연결, 뉴런당 평균 2.2개.
   나머지 1,075개는 연결이 끊긴 것이므로 자극 대상에서 제외한다.)
3. 한쪽 눈의 Mi1 soma 좌표에 PCA 를 걸어 껍질(shell) 위 2D 좌표 → (방위각, 고도) 로 만든다.
4. 공룡게임 화면을 그 (방위각, 고도) 격자에 투영해 컬럼별 휘도를 만든다.

## 3. 구현 계획

| 파일 | 내용 |
|---|---|
| `flybrain/retina.py` | 망막지도 구축(위 4단계) + 화면 → 광수용체 발화율 인코더. 결과는 캐시. |
| `flybrain/dino.py` | 크롬 공룡게임 물리(중력/점프/선인장 생성/속도 증가) + 휘도 프레임 렌더 |
| `flybrain/realtime.py` | 실시간 스케줄러. 생물시간 1초 = 벽시계 1초 유지, 지연(slip) 계측 |
| `flybrain/cli.py` | `flybrain dino` 명령 추가 |
| `results/dino_*.html` | 기존 `replay.py` 방식의 재생기 (게임 + 눈 입력 + LPLC2/DNp01 발화) |

설계 결정:
- **어두운 선인장 / 밝은 배경** → OFF 경로(L2/L3 → Tm1/Tm2 → T5 → LPLC2)가 구동된다.
  R1-6 은 이 모델에서 히스타민 = 억제이므로 부호가 생물학과 맞는다.
- 점프 판정은 기존 `MotorDecoder` 의 `escape`(DNp01, DNp11) / `takeoff`(DNp07, DNp10)
  그룹 발화율 임계값으로 한다. 몸 규칙(점프 후 불응 1초)은 이미 있다.
- 프레임당 생물시간 16.67 ms (60 fps) = 167 스텝. 매 프레임 인코드/디코드.
  탈출 반사 지연이 실제로 ~30~50 ms 이므로 게임 반응 시간(200~400 ms)에 충분하다.
- 게임 난이도(스크롤 속도)와 시각 이득은 튜닝 대상. 루밍이 약하면 DNp01 이 안 터진다.

## 4. 검증

- 망막지도: 컬럼 격자를 그림으로 뽑아 벌집 구조가 나오는지 눈으로 확인.
- 모션 검출: 좌→우, 우→좌 이동 격자(drifting grating)를 넣어 T4/T5 가 방향 선택성을
  보이는지 (T4a~d 는 4방향). 이게 되면 시각 경로가 진짜로 작동하는 것이다.
- 루밍: 확대되는 검은 원(looming disc)에 LPLC2 → DNp01 이 반응하는지.
  문헌값은 충돌 예정 시각 직전 LPLC2 피크.
- 실시간: slip 통계(프레임당 예산 16.67 ms 대비 실제 소요)를 리포트.
- 게임: 생존 시간 / 넘은 선인장 수. DNp01 을 silence 하면 즉시 죽어야 한다(음성 대조).
